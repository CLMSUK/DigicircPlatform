// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.

using System;
using System.Collections.Generic;
using System.Linq;
using NUnit.Framework;
using FluentNHibernate.Testing;
using DigicircMatchmaking.BO;
using DigicircMatchmaking.BoTesting.Tests.Common;
using DigicircMatchmaking.DAL;

namespace DigicircMatchmaking.BoTesting.Tests
{
    /// <summary>
    ///This is a test class for EntityTypeTest and is intended
    ///to contain all EntityTypeTest NUnit Tests
    ///</summary>
    [TestFixture]
    [Description("EntityType Repository Tests")]
    [Category("EntityType Repository Tests")]
    internal class EntityTypeTests : NHibernateFixture
    {
        /// <summary>
        ///A test for testing the persistence of an object from Db
        ///</summary>
        [Test]
        [Description("This tests the persistence of `EntityType`")]
        [Order(0)]
        public void EntityType_persistence_test()
        {
            DateTime now = DateTime.Now;
            // Get datetime without milliseconds
            now = new DateTime(now.Ticks - (now.Ticks % TimeSpan.TicksPerSecond), now.Kind);
            new PersistenceSpecification<DigicircMatchmaking.BO.EntityType>(Session)
            .CheckProperty(p => p.IsProvider, true)
            .CheckProperty(p => p.IsCluster, true)
            .CheckProperty(p => p.Code, "EntityType_Code")
            .CheckProperty(p => p.Value, "EntityType_Value")
            .VerifyTheMappings();
        }

        /// <summary>
        ///A test for testing the filtering of an object from Db
        ///</summary>
        [Test]
        [Description("This tests the filtering of `EntityType`")]
        [Order(1)]
        public void EntityType_filtering_test()
        {
            DateTime now = DateTime.Now;
            // Get datetime without milliseconds
            now = new DateTime(now.Ticks - (now.Ticks % TimeSpan.TicksPerSecond), now.Kind);
            var repo = new Repository(Session);
            List<DigicircMatchmaking.BO.EntityType> results = null;
            Assert.DoesNotThrow(() =>
            {
                results = repo.GetAsQueryable<DigicircMatchmaking.BO.EntityType>(
                              a => true
                              && !a.IsProvider
                              && !a.IsCluster
                              && (a.Code != string.Empty && a.Code != null)
                              && (a.Value != string.Empty && a.Value != null)
                              ,
                              cacheQuery: true)
                          .OrderBy(a => a)
                          .Skip(0)
                          .Take(3)
                          .ToList();
            });
            Assert.AreNotEqual(null, results);
        }
    }
}