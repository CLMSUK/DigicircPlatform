// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.

using System;
using System.Collections.Generic;
using System.Linq;
using NUnit.Framework;
using FluentNHibernate.Testing;
using DigicircMatchmaking.BO;
using DigicircMatchmaking.BoTesting.Tests.Common;
using DigicircMatchmaking.DAL;

namespace DigicircMatchmaking.BoTesting.Tests
{
    /// <summary>
    ///This is a test class for CompanyTest and is intended
    ///to contain all CompanyTest NUnit Tests
    ///</summary>
    [TestFixture]
    [Description("Company Repository Tests")]
    [Category("Company Repository Tests")]
    internal class CompanyTests : NHibernateFixture
    {
        /// <summary>
        ///A test for testing the persistence of an object from Db
        ///</summary>
        [Test]
        [Description("This tests the persistence of `Company`")]
        [Order(0)]
        public void Company_persistence_test()
        {
            DateTime now = DateTime.Now;
            // Get datetime without milliseconds
            now = new DateTime(now.Ticks - (now.Ticks % TimeSpan.TicksPerSecond), now.Kind);
            new PersistenceSpecification<DigicircMatchmaking.BO.Company>(Session)
            .CheckProperty(p => p.company_name, "Company_company_name")
            .CheckProperty(p => p.url, "Company_url")
            .CheckProperty(p => p.city, "Company_city")
            .CheckProperty(p => p.country, "Company_country")
            .CheckProperty(p => p.zip_code, "Company_zip_code")
            .CheckProperty(p => p.company_category, "Company_company_category")
            .CheckProperty(p => p.description, "Company_description")
            .CheckProperty(p => p.imported, true)
            .VerifyTheMappings();
        }

        /// <summary>
        ///A test for testing the filtering of an object from Db
        ///</summary>
        [Test]
        [Description("This tests the filtering of `Company`")]
        [Order(1)]
        public void Company_filtering_test()
        {
            DateTime now = DateTime.Now;
            // Get datetime without milliseconds
            now = new DateTime(now.Ticks - (now.Ticks % TimeSpan.TicksPerSecond), now.Kind);
            var repo = new Repository(Session);
            List<DigicircMatchmaking.BO.Company> results = null;
            Assert.DoesNotThrow(() =>
            {
                results = repo.GetAsQueryable<DigicircMatchmaking.BO.Company>(
                              a => true
                              && (a.company_name != string.Empty && a.company_name != null)
                              && (a.url != string.Empty && a.url != null)
                              && (a.city != string.Empty && a.city != null)
                              && (a.country != string.Empty && a.country != null)
                              && (a.zip_code != string.Empty && a.zip_code != null)
                              && (a.company_category != string.Empty && a.company_category != null)
                              && (a.description != string.Empty && a.description != null)
                              && !a.imported
                              ,
                              cacheQuery: true)
                          .OrderBy(a => a)
                          .Skip(0)
                          .Take(3)
                          .ToList();
            });
            Assert.AreNotEqual(null, results);
        }
    }
}