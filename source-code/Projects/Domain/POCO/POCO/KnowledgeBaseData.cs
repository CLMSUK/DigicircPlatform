// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DigicircMatchmaking.BO
{
    /// <summary>
    /// The KnowledgeBaseData class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class KnowledgeBaseData
    {
        #region KnowledgeBaseData's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="KnowledgeBaseDataKey")]
        protected int? knowledgeBaseDataKey = 0;
#pragma warning disable 0649
        private bool disableInternalAdditions;
#pragma warning restore 0649
        #endregion
        #region KnowledgeBaseData's Properties
/// <summary>
/// The KnowledgeBaseDataKey property
///
/// </summary>
///
        [Key]
        public virtual int? KnowledgeBaseDataKey
        {
            get
            {
                return knowledgeBaseDataKey;
            }
            set
            {
                knowledgeBaseDataKey = value;
            }
        }
        #endregion
        #region KnowledgeBaseData's Participant Properties
        [DataMember(Name="Row")]
        protected IList<KnowledgeBaseRow> row = new List<KnowledgeBaseRow>();
        public virtual List<KnowledgeBaseRow> Row
        {
            get
            {
                if (row is KnowledgeBaseRow[])
                {
                    row = row.ToList();
                }
                if (row == null)
                {
                    row = new List<KnowledgeBaseRow>();
                }
                return row.ToList();
            }
            set
            {
                if (row is KnowledgeBaseRow[])
                {
                    row = row.ToList();
                }
                if (row != null)
                {
                    var __itemsToDelete = new List<KnowledgeBaseRow>(row);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveRow(__item);
                    }
                }
                if(value == null)
                {
                    row = new List<KnowledgeBaseRow>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddRow(__item);
                }
            }
        }
        public virtual void AddRow(IList<KnowledgeBaseRow> __items)
        {
            foreach (var __item in __items)
            {
                AddRow(__item);
            }
        }

        public virtual void InternalAddRow(KnowledgeBaseRow __item)
        {
            if (__item == null || disableInternalAdditions) return;
            row?.Add(__item);
        }

        public virtual void InternalRemoveRow(KnowledgeBaseRow __item)
        {
            if (__item == null) return;
            row?.Remove(__item);
        }

        public virtual void AddRow(KnowledgeBaseRow __item)
        {
            if (__item == null) return;
            InternalAddRow(__item);
        }

        public virtual void AddAtIndexRow(int index, KnowledgeBaseRow __item)
        {
            if (__item == null) return;
            row?.Insert(index, __item);
        }

        public virtual void RemoveRow(KnowledgeBaseRow __item)
        {
            if (__item != null)
            {
                InternalRemoveRow(__item);
            }
        }
        public virtual void SetRowAt(KnowledgeBaseRow __item, int __index)
        {
            if (__item == null)
            {
                row[__index] = null;
            }
            else
            {
                row[__index] = __item;
            }
        }

        public virtual void ClearRow()
        {
            if (row!=null)
            {
                var __itemsToRemove = row.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveRow(__item);
                }
            }
        }
        [DataMember(Name="Meta")]
        protected IList<KnowledgeBaseMeta> meta = new List<KnowledgeBaseMeta>();
        public virtual List<KnowledgeBaseMeta> Meta
        {
            get
            {
                if (meta is KnowledgeBaseMeta[])
                {
                    meta = meta.ToList();
                }
                if (meta == null)
                {
                    meta = new List<KnowledgeBaseMeta>();
                }
                return meta.ToList();
            }
            set
            {
                if (meta is KnowledgeBaseMeta[])
                {
                    meta = meta.ToList();
                }
                if (meta != null)
                {
                    var __itemsToDelete = new List<KnowledgeBaseMeta>(meta);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveMeta(__item);
                    }
                }
                if(value == null)
                {
                    meta = new List<KnowledgeBaseMeta>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddMeta(__item);
                }
            }
        }
        public virtual void AddMeta(IList<KnowledgeBaseMeta> __items)
        {
            foreach (var __item in __items)
            {
                AddMeta(__item);
            }
        }

        public virtual void InternalAddMeta(KnowledgeBaseMeta __item)
        {
            if (__item == null || disableInternalAdditions) return;
            meta?.Add(__item);
        }

        public virtual void InternalRemoveMeta(KnowledgeBaseMeta __item)
        {
            if (__item == null) return;
            meta?.Remove(__item);
        }

        public virtual void AddMeta(KnowledgeBaseMeta __item)
        {
            if (__item == null) return;
            InternalAddMeta(__item);
        }

        public virtual void AddAtIndexMeta(int index, KnowledgeBaseMeta __item)
        {
            if (__item == null) return;
            meta?.Insert(index, __item);
        }

        public virtual void RemoveMeta(KnowledgeBaseMeta __item)
        {
            if (__item != null)
            {
                InternalRemoveMeta(__item);
            }
        }
        public virtual void SetMetaAt(KnowledgeBaseMeta __item, int __index)
        {
            if (__item == null)
            {
                meta[__index] = null;
            }
            else
            {
                meta[__index] = __item;
            }
        }

        public virtual void ClearMeta()
        {
            if (meta!=null)
            {
                var __itemsToRemove = meta.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveMeta(__item);
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the KnowledgeBaseData class
/// </summary>
/// <returns>New KnowledgeBaseData object</returns>
/// <remarks></remarks>
        public KnowledgeBaseData() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'KnowledgeBaseData' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (KnowledgeBaseDataKey?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [KnowledgeBaseData] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual KnowledgeBaseData Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, KnowledgeBaseData copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (KnowledgeBaseData)copiedObjects[this];
            copy = copy ?? new KnowledgeBaseData();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.KnowledgeBaseDataKey = this.KnowledgeBaseDataKey;
            }
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            copy.row = new List<KnowledgeBaseRow>();
            if(deep && this.row != null)
            {
                foreach (var __item in this.row)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddRow(__item);
                        else
                            copy.AddRow(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddRow((KnowledgeBaseRow)copiedObjects[__item]);
                    }
                }
            }
            copy.meta = new List<KnowledgeBaseMeta>();
            if(deep && this.meta != null)
            {
                foreach (var __item in this.meta)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddMeta(__item);
                        else
                            copy.AddMeta(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddMeta((KnowledgeBaseMeta)copiedObjects[__item]);
                    }
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as KnowledgeBaseData;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(KnowledgeBaseData x, KnowledgeBaseData y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(KnowledgeBaseData x, KnowledgeBaseData y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("KnowledgeBaseDataKey");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.KnowledgeBaseDataKey.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.KnowledgeBaseDataKey == default(int) || this.KnowledgeBaseDataKey.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(KnowledgeBaseData compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.KnowledgeBaseDataKey.Equals(compareTo.KnowledgeBaseDataKey);
        }

        #endregion


    }
}
