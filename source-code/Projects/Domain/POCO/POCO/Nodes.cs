// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DigicircMatchmaking.BO
{
    /// <summary>
    /// The Nodes class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class Nodes
    {
        #region Nodes's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="Name")]
        protected string name;
        [DataMember(Name="Label")]
        protected string label;
        [DataMember(Name="LabelType")]
        protected string labelType;
        [DataMember(Name="Id")]
        protected int? id;
        [DataMember(Name="Graphid")]
        protected int? graphid;
        [DataMember(Name="CC")]
        protected bool cC;
        [DataMember(Name="SL")]
        protected int? sL;
        [DataMember(Name="CL")]
        protected float? cL;
        [DataMember(Name="RL")]
        protected float? rL;
        [DataMember(Name="IA")]
        protected bool iA;
        [DataMember(Name="AL")]
        protected int? aL;
        [DataMember(Name="AC")]
        protected float? aC;
        [DataMember(Name="Attr")]
        protected bool attr;
        [DataMember(Name="NodesKey")]
        protected int? nodesKey = 0;
        #endregion
        #region Nodes's Properties
/// <summary>
/// The Name property
///
/// </summary>
///
        public virtual string Name
        {
            get
            {
                return name;
            }
            set
            {
                name = value;
            }
        }
/// <summary>
/// The Label property
///
/// </summary>
///
        public virtual string Label
        {
            get
            {
                return label;
            }
            set
            {
                label = value;
            }
        }
/// <summary>
/// The LabelType property
///
/// </summary>
///
        public virtual string LabelType
        {
            get
            {
                return labelType;
            }
            set
            {
                labelType = value;
            }
        }
/// <summary>
/// The Id property
///
/// </summary>
///
        public virtual int? Id
        {
            get
            {
                return id;
            }
            set
            {
                id = value;
            }
        }
/// <summary>
/// The Graphid property
///
/// </summary>
///
        public virtual int? Graphid
        {
            get
            {
                return graphid;
            }
            set
            {
                graphid = value;
            }
        }
/// <summary>
/// The CC property
///
/// </summary>
///
        public virtual bool CC
        {
            get
            {
                return cC;
            }
            set
            {
                cC = value;
            }
        }
/// <summary>
/// The SL property
///
/// </summary>
///
        public virtual int? SL
        {
            get
            {
                return sL;
            }
            set
            {
                sL = value;
            }
        }
/// <summary>
/// The CL property
///
/// </summary>
///
        public virtual float? CL
        {
            get
            {
                return cL;
            }
            set
            {
                cL = value;
            }
        }
/// <summary>
/// The RL property
///
/// </summary>
///
        public virtual float? RL
        {
            get
            {
                return rL;
            }
            set
            {
                rL = value;
            }
        }
/// <summary>
/// The IA property
///
/// </summary>
///
        public virtual bool IA
        {
            get
            {
                return iA;
            }
            set
            {
                iA = value;
            }
        }
/// <summary>
/// The AL property
///
/// </summary>
///
        public virtual int? AL
        {
            get
            {
                return aL;
            }
            set
            {
                aL = value;
            }
        }
/// <summary>
/// The AC property
///
/// </summary>
///
        public virtual float? AC
        {
            get
            {
                return aC;
            }
            set
            {
                aC = value;
            }
        }
/// <summary>
/// The Attr property
///
/// </summary>
///
        public virtual bool Attr
        {
            get
            {
                return attr;
            }
            set
            {
                attr = value;
            }
        }
/// <summary>
/// The NodesKey property
///
/// </summary>
///
        [Key]
        public virtual int? NodesKey
        {
            get
            {
                return nodesKey;
            }
            set
            {
                nodesKey = value;
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the Nodes class
/// </summary>
/// <returns>New Nodes object</returns>
/// <remarks></remarks>
        public Nodes() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (Name != null && Name.Length > 100)
            {
                __errors.Add("Length of property 'Name' cannot be greater than 100.");
            }
            if (Label != null && Label.Length > 100)
            {
                __errors.Add("Length of property 'Label' cannot be greater than 100.");
            }
            if (LabelType != null && LabelType.Length > 100)
            {
                __errors.Add("Length of property 'LabelType' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'Nodes' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (Name?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Label?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (LabelType?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Graphid?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (CC.GetHashCode() );
            hashCode = hashCode * -1521134295 + (SL?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (CL?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (RL?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (IA.GetHashCode() );
            hashCode = hashCode * -1521134295 + (AL?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (AC?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Attr.GetHashCode() );
            hashCode = hashCode * -1521134295 + (NodesKey?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [Nodes] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual Nodes Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, Nodes copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (Nodes)copiedObjects[this];
            copy = copy ?? new Nodes();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.NodesKey = this.NodesKey;
            }
            copy.Name = this.Name;
            copy.Label = this.Label;
            copy.LabelType = this.LabelType;
            copy.Id = this.Id;
            copy.Graphid = this.Graphid;
            copy.CC = this.CC;
            copy.SL = this.SL;
            copy.CL = this.CL;
            copy.RL = this.RL;
            copy.IA = this.IA;
            copy.AL = this.AL;
            copy.AC = this.AC;
            copy.Attr = this.Attr;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as Nodes;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(Nodes x, Nodes y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(Nodes x, Nodes y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("NodesKey");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.NodesKey.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.NodesKey == default(int) || this.NodesKey.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(Nodes compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.NodesKey.Equals(compareTo.NodesKey);
        }

        #endregion


    }
}
