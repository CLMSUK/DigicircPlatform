// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DigicircMatchmaking.BO
{
    /// <summary>
    /// The GraphBackendResponse class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class GraphBackendResponse
    {
        #region GraphBackendResponse's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="GraphBackendResponseKey")]
        protected int? graphBackendResponseKey = 0;
#pragma warning disable 0649
        private bool disableInternalAdditions;
#pragma warning restore 0649
        #endregion
        #region GraphBackendResponse's Properties
/// <summary>
/// The GraphBackendResponseKey property
///
/// </summary>
///
        [Key]
        public virtual int? GraphBackendResponseKey
        {
            get
            {
                return graphBackendResponseKey;
            }
            set
            {
                graphBackendResponseKey = value;
            }
        }
        #endregion
        #region GraphBackendResponse's Participant Properties
        [DataMember(Name="Metadata")]
        protected Metadata metadata;
        public virtual Metadata Metadata
        {
            get
            {
                return metadata;
            }
            set
            {
                if(Equals(metadata, value)) return;
                var __oldValue = metadata;
                if (value != null)
                {
                    metadata = value;
                }
                else
                {
                    if (metadata != null)
                    {
                        metadata = null;
                    }
                }
            }
        }
        [DataMember(Name="Nodes")]
        protected IList<Nodes> nodes = new List<Nodes>();
        public virtual List<Nodes> Nodes
        {
            get
            {
                if (nodes is Nodes[])
                {
                    nodes = nodes.ToList();
                }
                if (nodes == null)
                {
                    nodes = new List<Nodes>();
                }
                return nodes.ToList();
            }
            set
            {
                if (nodes is Nodes[])
                {
                    nodes = nodes.ToList();
                }
                if (nodes != null)
                {
                    var __itemsToDelete = new List<Nodes>(nodes);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveNodes(__item);
                    }
                }
                if(value == null)
                {
                    nodes = new List<Nodes>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddNodes(__item);
                }
            }
        }
        public virtual void AddNodes(IList<Nodes> __items)
        {
            foreach (var __item in __items)
            {
                AddNodes(__item);
            }
        }

        public virtual void InternalAddNodes(Nodes __item)
        {
            if (__item == null || disableInternalAdditions) return;
            nodes?.Add(__item);
        }

        public virtual void InternalRemoveNodes(Nodes __item)
        {
            if (__item == null) return;
            nodes?.Remove(__item);
        }

        public virtual void AddNodes(Nodes __item)
        {
            if (__item == null) return;
            InternalAddNodes(__item);
        }

        public virtual void AddAtIndexNodes(int index, Nodes __item)
        {
            if (__item == null) return;
            nodes?.Insert(index, __item);
        }

        public virtual void RemoveNodes(Nodes __item)
        {
            if (__item != null)
            {
                InternalRemoveNodes(__item);
            }
        }
        public virtual void SetNodesAt(Nodes __item, int __index)
        {
            if (__item == null)
            {
                nodes[__index] = null;
            }
            else
            {
                nodes[__index] = __item;
            }
        }

        public virtual void ClearNodes()
        {
            if (nodes!=null)
            {
                var __itemsToRemove = nodes.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveNodes(__item);
                }
            }
        }
        [DataMember(Name="Links")]
        protected IList<Links> links = new List<Links>();
        public virtual List<Links> Links
        {
            get
            {
                if (links is Links[])
                {
                    links = links.ToList();
                }
                if (links == null)
                {
                    links = new List<Links>();
                }
                return links.ToList();
            }
            set
            {
                if (links is Links[])
                {
                    links = links.ToList();
                }
                if (links != null)
                {
                    var __itemsToDelete = new List<Links>(links);
                    foreach (var __item in __itemsToDelete)
                    {
                        RemoveLinks(__item);
                    }
                }
                if(value == null)
                {
                    links = new List<Links>();
                    return;
                }
                foreach(var __item in value)
                {
                    AddLinks(__item);
                }
            }
        }
        public virtual void AddLinks(IList<Links> __items)
        {
            foreach (var __item in __items)
            {
                AddLinks(__item);
            }
        }

        public virtual void InternalAddLinks(Links __item)
        {
            if (__item == null || disableInternalAdditions) return;
            links?.Add(__item);
        }

        public virtual void InternalRemoveLinks(Links __item)
        {
            if (__item == null) return;
            links?.Remove(__item);
        }

        public virtual void AddLinks(Links __item)
        {
            if (__item == null) return;
            InternalAddLinks(__item);
        }

        public virtual void AddAtIndexLinks(int index, Links __item)
        {
            if (__item == null) return;
            links?.Insert(index, __item);
        }

        public virtual void RemoveLinks(Links __item)
        {
            if (__item != null)
            {
                InternalRemoveLinks(__item);
            }
        }
        public virtual void SetLinksAt(Links __item, int __index)
        {
            if (__item == null)
            {
                links[__index] = null;
            }
            else
            {
                links[__index] = __item;
            }
        }

        public virtual void ClearLinks()
        {
            if (links!=null)
            {
                var __itemsToRemove = links.ToList();
                foreach(var __item in __itemsToRemove)
                {
                    RemoveLinks(__item);
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the GraphBackendResponse class
/// </summary>
/// <returns>New GraphBackendResponse object</returns>
/// <remarks></remarks>
        public GraphBackendResponse() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'GraphBackendResponse' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (GraphBackendResponseKey?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [GraphBackendResponse] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual GraphBackendResponse Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, GraphBackendResponse copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (GraphBackendResponse)copiedObjects[this];
            copy = copy ?? new GraphBackendResponse();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.GraphBackendResponseKey = this.GraphBackendResponseKey;
            }
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this.metadata != null)
            {
                if (!copiedObjects.Contains(this.metadata))
                {
                    if (asNew && reuseNestedObjects)
                        copy.Metadata = this.Metadata;
                    else if (asNew)
                        copy.Metadata = this.Metadata.Copy(deep, copiedObjects, true);
                    else
                        copy.metadata = this.metadata.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.Metadata = (Metadata)copiedObjects[this.Metadata];
                    else
                        copy.metadata = (Metadata)copiedObjects[this.Metadata];
                }
            }
            copy.nodes = new List<Nodes>();
            if(deep && this.nodes != null)
            {
                foreach (var __item in this.nodes)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddNodes(__item);
                        else
                            copy.AddNodes(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddNodes((Nodes)copiedObjects[__item]);
                    }
                }
            }
            copy.links = new List<Links>();
            if(deep && this.links != null)
            {
                foreach (var __item in this.links)
                {
                    if (!copiedObjects.Contains(__item))
                    {
                        if (asNew && reuseNestedObjects)
                            copy.AddLinks(__item);
                        else
                            copy.AddLinks(__item.Copy(deep, copiedObjects, asNew));
                    }
                    else
                    {
                        copy.AddLinks((Links)copiedObjects[__item]);
                    }
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as GraphBackendResponse;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(GraphBackendResponse x, GraphBackendResponse y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(GraphBackendResponse x, GraphBackendResponse y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("GraphBackendResponseKey");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.GraphBackendResponseKey.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.GraphBackendResponseKey == default(int) || this.GraphBackendResponseKey.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(GraphBackendResponse compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.GraphBackendResponseKey.Equals(compareTo.GraphBackendResponseKey);
        }

        #endregion


    }
}
