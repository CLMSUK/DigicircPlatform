// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DigicircMatchmaking.BO
{
    /// <summary>
    /// The Address class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class Address : IDomainModelClass
    {
        #region Address's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="Id")]
        protected int? id = 0;
        [DataMember(Name="Latitude")]
        protected double? latitude;
        [DataMember(Name="Longitude")]
        protected double? longitude;
        [DataMember(Name="StreetName")]
        protected string streetName;
        [DataMember(Name="Number")]
        protected string number;
        [DataMember(Name="Town")]
        protected string town;
        [DataMember(Name="Zip")]
        protected string zip;
        [DataMember(Name="FullAddress")]
        protected string fullAddress;
        [DataMember(Name="Alias")]
        protected string alias;
        [DataMember(Name="VersionTimestamp")]
        protected int? versionTimestamp;

        #endregion
        #region Address's Properties
/// <summary>
/// The Id property
///
/// </summary>
///
        [Key]
        public virtual int? Id
        {
            get
            {
                return id;
            }
            set
            {
                id = value;
            }
        }
/// <summary>
/// The Latitude property
///
/// </summary>
///
        public virtual double? Latitude
        {
            get
            {
                return latitude;
            }
            set
            {
                latitude = value;
            }
        }
/// <summary>
/// The Longitude property
///
/// </summary>
///
        public virtual double? Longitude
        {
            get
            {
                return longitude;
            }
            set
            {
                longitude = value;
            }
        }
/// <summary>
/// The StreetName property
///
/// </summary>
///
        public virtual string StreetName
        {
            get
            {
                return streetName;
            }
            set
            {
                streetName = value;
            }
        }
/// <summary>
/// The Number property
///
/// </summary>
///
        public virtual string Number
        {
            get
            {
                return number;
            }
            set
            {
                number = value;
            }
        }
/// <summary>
/// The Town property
///
/// </summary>
///
        public virtual string Town
        {
            get
            {
                return town;
            }
            set
            {
                town = value;
            }
        }
/// <summary>
/// The Zip property
///
/// </summary>
///
        public virtual string Zip
        {
            get
            {
                return zip;
            }
            set
            {
                zip = value;
            }
        }
/// <summary>
/// The FullAddress property
///
/// </summary>
///
        public virtual string FullAddress
        {
            get
            {
                var __valToGet = GetFullAddress();
                return __valToGet;
            }
            set
            {
                fullAddress = value;
            }
        }
/// <summary>
/// The Alias property
///
/// </summary>
///
        public virtual string Alias
        {
            get
            {
                return alias;
            }
            set
            {
                alias = value;
            }
        }
/// <summary>
/// The VersionTimestamp property
///Provides concurrency control for the class
/// </summary>
///
        public virtual int? VersionTimestamp
        {
            get
            {
                return versionTimestamp;
            }
            set
            {
                versionTimestamp = value;
            }
        }
        #endregion
        #region Address's Participant Properties
        [DataMember(Name="Country")]
        protected Country country;
        public virtual Country Country
        {
            get
            {
                return country;
            }
            set
            {
                if(Equals(country, value)) return;
                var __oldValue = country;
                country = value;
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the Address class
/// </summary>
/// <returns>New Address object</returns>
/// <remarks></remarks>
        public Address() {}
        #endregion
        #region Accessors Implementation
        private string GetFullAddress()
        {
            return (this?.streetName ?? "") + " " + (this?.number ?? "") + ",<br />" + (this?.town ?? "") + " " + (this?.zip ?? "") + "<br />" + (this?.country?.Name ?? "");
        }

        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (Id == null)
            {
                __errors.Add("Property 'Id' is required.");
            }
            if (StreetName != null && StreetName.Length > 100)
            {
                __errors.Add("Length of property 'StreetName' cannot be greater than 100.");
            }
            if (Number != null && Number.Length > 100)
            {
                __errors.Add("Length of property 'Number' cannot be greater than 100.");
            }
            if (Town != null && Town.Length > 100)
            {
                __errors.Add("Length of property 'Town' cannot be greater than 100.");
            }
            if (Zip != null && Zip.Length > 100)
            {
                __errors.Add("Length of property 'Zip' cannot be greater than 100.");
            }
            if (FullAddress != null && FullAddress.Length > 2147483647)
            {
                __errors.Add("Length of property 'FullAddress' cannot be greater than 2147483647.");
            }
            if (Alias != null && Alias.Length > 500)
            {
                __errors.Add("Length of property 'Alias' cannot be greater than 500.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'Address' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (Id?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Latitude?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Longitude?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (StreetName?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Number?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Town?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Zip?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (FullAddress?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (Alias?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [Address] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual Address Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, Address copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (Address)copiedObjects[this];
            copy = copy ?? new Address();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.Id = this.Id;
            }
            copy.Latitude = this.Latitude;
            copy.Longitude = this.Longitude;
            copy.StreetName = this.StreetName;
            copy.Number = this.Number;
            copy.Town = this.Town;
            copy.Zip = this.Zip;
            copy.FullAddress = this.FullAddress;
            copy.Alias = this.Alias;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this.country != null)
            {
                if (!copiedObjects.Contains(this.country))
                {
                    if (asNew && reuseNestedObjects)
                        copy.Country = this.Country;
                    else if (asNew)
                        copy.Country = this.Country.Copy(deep, copiedObjects, true);
                    else
                        copy.country = this.country.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.Country = (Country)copiedObjects[this.Country];
                    else
                        copy.country = (Country)copiedObjects[this.Country];
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as Address;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(Address x, Address y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(Address x, Address y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("Id");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.Id.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.Id == default(int) || this.Id.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(Address compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.Id.Equals(compareTo.Id);
        }

        #endregion


    }
}
