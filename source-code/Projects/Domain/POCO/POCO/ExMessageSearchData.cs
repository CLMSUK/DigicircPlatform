// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
using System;
using System.Runtime.Serialization;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Reflection;
using System.Collections;
using System.ComponentModel.DataAnnotations;
using System.Linq.Expressions;
using zAppDev.DotNet.Framework.Linq;
using zAppDev.DotNet.Framework.Data.Domain;
using zAppDev.DotNet.Framework.Workflow;
using zAppDev.DotNet.Framework.Identity.Model;

namespace DigicircMatchmaking.BO
{
    /// <summary>
    /// The ExMessageSearchData class
    ///
    /// </summary>
    [Serializable]
    [DataContract]
    public class ExMessageSearchData
    {
        #region ExMessageSearchData's Fields

        protected Guid _transientId= Guid.NewGuid();
        public virtual Guid TransientId
        {
            get
            {
                return _transientId;
            }
            set
            {
                _transientId = value;
            }
        }
        [DataMember(Name="LabelSearchType")]
        protected string labelSearchType;
        [DataMember(Name="DepthSearchLevel")]
        protected string depthSearchLevel;
        [DataMember(Name="AlgorithmName")]
        protected string algorithmName;
        [DataMember(Name="JsonResponseFormat")]
        protected string jsonResponseFormat;
        [DataMember(Name="FindPathTypes")]
        protected string findPathTypes;
        [DataMember(Name="AlgorithmOptimalPathParameters")]
        protected string algorithmOptimalPathParameters;
        [DataMember(Name="ExMessageSearchDataKey")]
        protected int? exMessageSearchDataKey = 0;
        #endregion
        #region ExMessageSearchData's Properties
/// <summary>
/// The LabelSearchType property
///
/// </summary>
///
        public virtual string LabelSearchType
        {
            get
            {
                return labelSearchType;
            }
            set
            {
                labelSearchType = value;
            }
        }
/// <summary>
/// The DepthSearchLevel property
///
/// </summary>
///
        public virtual string DepthSearchLevel
        {
            get
            {
                return depthSearchLevel;
            }
            set
            {
                depthSearchLevel = value;
            }
        }
/// <summary>
/// The AlgorithmName property
///
/// </summary>
///
        public virtual string AlgorithmName
        {
            get
            {
                return algorithmName;
            }
            set
            {
                algorithmName = value;
            }
        }
/// <summary>
/// The JsonResponseFormat property
///
/// </summary>
///
        public virtual string JsonResponseFormat
        {
            get
            {
                return jsonResponseFormat;
            }
            set
            {
                jsonResponseFormat = value;
            }
        }
/// <summary>
/// The FindPathTypes property
///
/// </summary>
///
        public virtual string FindPathTypes
        {
            get
            {
                return findPathTypes;
            }
            set
            {
                findPathTypes = value;
            }
        }
/// <summary>
/// The AlgorithmOptimalPathParameters property
///
/// </summary>
///
        public virtual string AlgorithmOptimalPathParameters
        {
            get
            {
                return algorithmOptimalPathParameters;
            }
            set
            {
                algorithmOptimalPathParameters = value;
            }
        }
/// <summary>
/// The ExMessageSearchDataKey property
///
/// </summary>
///
        [Key]
        public virtual int? ExMessageSearchDataKey
        {
            get
            {
                return exMessageSearchDataKey;
            }
            set
            {
                exMessageSearchDataKey = value;
            }
        }
        #endregion
        #region ExMessageSearchData's Participant Properties
        [DataMember(Name="ExSecurity")]
        protected ExSecurity exSecurity;
        public virtual ExSecurity ExSecurity
        {
            get
            {
                return exSecurity;
            }
            set
            {
                if(Equals(exSecurity, value)) return;
                var __oldValue = exSecurity;
                if (value != null)
                {
                    exSecurity = value;
                }
                else
                {
                    if (exSecurity != null)
                    {
                        exSecurity = null;
                    }
                }
            }
        }
        #endregion
        #region Constructors
/// <summary>
/// Public constructors of the ExMessageSearchData class
/// </summary>
/// <returns>New ExMessageSearchData object</returns>
/// <remarks></remarks>
        public ExMessageSearchData() {}
        #endregion
        #region Methods

        public virtual List<string> _Validate(bool throwException = true)
        {
            var __errors = new List<string>();
            if (LabelSearchType != null && LabelSearchType.Length > 100)
            {
                __errors.Add("Length of property 'LabelSearchType' cannot be greater than 100.");
            }
            if (DepthSearchLevel != null && DepthSearchLevel.Length > 100)
            {
                __errors.Add("Length of property 'DepthSearchLevel' cannot be greater than 100.");
            }
            if (AlgorithmName != null && AlgorithmName.Length > 100)
            {
                __errors.Add("Length of property 'AlgorithmName' cannot be greater than 100.");
            }
            if (JsonResponseFormat != null && JsonResponseFormat.Length > 100)
            {
                __errors.Add("Length of property 'JsonResponseFormat' cannot be greater than 100.");
            }
            if (FindPathTypes != null && FindPathTypes.Length > 100)
            {
                __errors.Add("Length of property 'FindPathTypes' cannot be greater than 100.");
            }
            if (AlgorithmOptimalPathParameters != null && AlgorithmOptimalPathParameters.Length > 100)
            {
                __errors.Add("Length of property 'AlgorithmOptimalPathParameters' cannot be greater than 100.");
            }
            if (throwException && __errors.Any())
            {
                throw new zAppDev.DotNet.Framework.Exceptions.BusinessException("An instance of TypeClass 'ExMessageSearchData' has validation errors:\r\n\r\n" + string.Join("\r\n", __errors));
            }
            return __errors;
        }

        public virtual int _GetUniqueIdentifier()
        {
            var hashCode = 399326290;
            hashCode = hashCode * -1521134295 + (LabelSearchType?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (DepthSearchLevel?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (AlgorithmName?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (JsonResponseFormat?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (FindPathTypes?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (AlgorithmOptimalPathParameters?.GetHashCode() ?? 0);
            hashCode = hashCode * -1521134295 + (ExMessageSearchDataKey?.GetHashCode() ?? 0);
            return hashCode;
        }






/// <summary>
/// Copies the current object to a new instance
/// </summary>
/// <param name="deep">Copy members that refer to objects external to this class (not dependent)</param>
/// <param name="copiedObjects">Objects that should be reused</param>
/// <param name="asNew">Copy the current object as a new one, ready to be persisted, along all its members.</param>
/// <param name="reuseNestedObjects">If asNew is true, this flag if set, forces the reuse of all external objects.</param>
/// <param name="copy">Optional - An existing [ExMessageSearchData] instance to use as the destination.</param>
/// <returns>A copy of the object</returns>
        public virtual ExMessageSearchData Copy(bool deep=false, Hashtable copiedObjects=null, bool asNew=false, bool reuseNestedObjects = false, ExMessageSearchData copy = null)
        {
            if(copiedObjects == null)
            {
                copiedObjects = new Hashtable();
            }
            if (copy == null && copiedObjects.Contains(this))
                return (ExMessageSearchData)copiedObjects[this];
            copy = copy ?? new ExMessageSearchData();
            if (!asNew)
            {
                copy.TransientId = this.TransientId;
                copy.ExMessageSearchDataKey = this.ExMessageSearchDataKey;
            }
            copy.LabelSearchType = this.LabelSearchType;
            copy.DepthSearchLevel = this.DepthSearchLevel;
            copy.AlgorithmName = this.AlgorithmName;
            copy.JsonResponseFormat = this.JsonResponseFormat;
            copy.FindPathTypes = this.FindPathTypes;
            copy.AlgorithmOptimalPathParameters = this.AlgorithmOptimalPathParameters;
            if (!copiedObjects.Contains(this))
            {
                copiedObjects.Add(this, copy);
            }
            if(deep && this.exSecurity != null)
            {
                if (!copiedObjects.Contains(this.exSecurity))
                {
                    if (asNew && reuseNestedObjects)
                        copy.ExSecurity = this.ExSecurity;
                    else if (asNew)
                        copy.ExSecurity = this.ExSecurity.Copy(deep, copiedObjects, true);
                    else
                        copy.exSecurity = this.exSecurity.Copy(deep, copiedObjects, false);
                }
                else
                {
                    if (asNew)
                        copy.ExSecurity = (ExSecurity)copiedObjects[this.ExSecurity];
                    else
                        copy.exSecurity = (ExSecurity)copiedObjects[this.ExSecurity];
                }
            }
            return copy;
        }

        public override bool Equals(object obj)
        {
            var compareTo = obj as ExMessageSearchData;
            if (ReferenceEquals(this, compareTo))
            {
                return true;
            }
            if (compareTo == null || !this.GetType().Equals(compareTo.GetTypeUnproxied()))
            {
                return false;
            }
            if (this.HasSameNonDefaultIdAs(compareTo))
            {
                return true;
            }
            // Since the Ids aren't the same, both of them must be transient to
            // compare domain signatures; because if one is transient and the
            // other is a persisted entity, then they cannot be the same object.
            return this.IsTransient() && compareTo.IsTransient() && (base.Equals(compareTo) || this.TransientId.Equals(compareTo.TransientId));
        }

// Maintain equality operator semantics for entities.
        public static bool operator ==(ExMessageSearchData x, ExMessageSearchData y)
        {
            // By default, == and Equals compares references. In order to
            // maintain these semantics with entities, we need to compare by
            // identity value. The Equals(x, y) override is used to guard
            // against null values; it then calls EntityEquals().
            return Equals(x, y);
        }

// Maintain inequality operator semantics for entities.
        public static bool operator !=(ExMessageSearchData x, ExMessageSearchData y)
        {
            return !(x == y);
        }

        private PropertyInfo __propertyKeyCache;
        public virtual PropertyInfo GetPrimaryKey()
        {
            if (__propertyKeyCache == null)
            {
                __propertyKeyCache = this.GetType().GetProperty("ExMessageSearchDataKey");
            }
            return __propertyKeyCache;
        }


/// <summary>
///     To help ensure hashcode uniqueness, a carefully selected random number multiplier
///     is used within the calculation.  Goodrich and Tamassia's Data Structures and
///     Algorithms in Java asserts that 31, 33, 37, 39 and 41 will produce the fewest number
///     of collissions.  See http://computinglife.wordpress.com/2008/11/20/why-do-hash-functions-use-prime-numbers/
///     for more information.
/// </summary>
        private const int HashMultiplier = 31;
        private int? cachedHashcode;

        public override int GetHashCode()
        {
            if (this.cachedHashcode.HasValue)
            {
                return this.cachedHashcode.Value;
            }
            if (this.IsTransient())
            {
                //this.cachedHashcode = base.GetHashCode();
                return this.TransientId.GetHashCode(); //don't cache because this won't stay transient forever
            }
            else
            {
                unchecked
                {
                    // It's possible for two objects to return the same hash code based on
                    // identically valued properties, even if they're of two different types,
                    // so we include the object's type in the hash calculation
                    var hashCode = this.GetType().GetHashCode();
                    this.cachedHashcode = (hashCode * HashMultiplier) ^ this.ExMessageSearchDataKey.GetHashCode();
                }
            }
            return this.cachedHashcode.Value;
        }

/// <summary>
///     Transient objects are not associated with an item already in storage.  For instance,
///     a Customer is transient if its Id is 0.  It's virtual to allow NHibernate-backed
///     objects to be lazily loaded.
/// </summary>
        public virtual bool IsTransient()
        {
            return this.ExMessageSearchDataKey == default(int) || this.ExMessageSearchDataKey.Equals(default(int));
        }

/// <summary>
///     When NHibernate proxies objects, it masks the type of the actual entity object.
///     This wrapper burrows into the proxied object to get its actual type.
///
///     Although this assumes NHibernate is being used, it doesn't require any NHibernate
///     related dependencies and has no bad side effects if NHibernate isn't being used.
///
///     Related discussion is at http://groups.google.com/group/sharp-architecture/browse_thread/thread/ddd05f9baede023a ...thanks Jay Oliver!
/// </summary>
        protected virtual System.Type GetTypeUnproxied()
        {
            return this.GetType();
        }

/// <summary>
///     Returns true if self and the provided entity have the same Id values
///     and the Ids are not of the default Id value
/// </summary>
        protected bool HasSameNonDefaultIdAs(ExMessageSearchData compareTo)
        {
            return !this.IsTransient() && !compareTo.IsTransient() && this.ExMessageSearchDataKey.Equals(compareTo.ExMessageSearchDataKey);
        }

        #endregion


    }
}
