// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
window.onbeforeunload = (e) => {
    if (window._context.isDirty) {
        var msg = window._resourcesManager.getGlobalResource("RES_WEBFORM_DirtyMessage");
        e.returnValue = msg;     // Gecko, Trident, Chrome 34+
        return msg;
    }
};
interface Window { scope_OportunitiesExplorer: DigicircMatchmaking.Controllers.IOportunitiesExplorerScope; }
namespace DigicircMatchmaking.Controllers {
const OportunitiesExplorerConditionalFormattings = [
        ];
const OportunitiesExplorerDataValidations = [
        ];
const OportunitiesExplorerCalculatedExpressions = [
        ];

// Datasets Column Info

// Datasets Projection Shemas


export interface IOportunitiesExplorerScope extends Joove.IWebPageScope {
model:
    DigicircMatchmaking.ViewModels.OportunitiesExplorer.OportunitiesExplorerViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,

    },
    _masterValidations?: any;
}

class OportunitiesExplorerController {


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_OportunitiesExplorer.model = window.scope_OportunitiesExplorer.dehydrate();
        //window.scope_MasterPage.model = window.scope_OportunitiesExplorer.model;
        DigicircMatchmaking.ViewModels.OportunitiesExplorer.OportunitiesExplorerViewModel._lightCast(window.scope_OportunitiesExplorer.model);
        // Joove.Common.applyScope(window.scope_OportunitiesExplorer);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: IOportunitiesExplorerScope, $timeout: ng.ITimeoutService) {
        window.scope_OportunitiesExplorer = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DigicircMatchmaking.ViewModels.OportunitiesExplorer.OportunitiesExplorerViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_MasterPage._validations;
        $scope._validations = { summary:
                                [],
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: IOportunitiesExplorerScope, $timeout: ng.ITimeoutService) {


        $scope.model = DigicircMatchmaking.ViewModels.OportunitiesExplorer.OportunitiesExplorerViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_MasterPage.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);




        //}

        $.connection['eventsHub'].on('__connectedEvent', () => { });

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start().then(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {
        };
// Controller actions
        $scope.actions = {
Index:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("OportunitiesExplorer", "Index", "GET", [], {}, _modalOptions);
            },
        };
// Events
        $scope.eventCallbacks = {
        };

// Rules
        window._ruleEngine.addDataValidations(OportunitiesExplorerDataValidations);
        window._ruleEngine.addConditionalFormattings(OportunitiesExplorerConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(OportunitiesExplorerCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_MasterPage as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("OportunitiesExplorerController", ["$scope", "$timeout", OportunitiesExplorerController] as Array<string>);
}
