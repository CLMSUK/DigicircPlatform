// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
window.onbeforeunload = (e) => {
    if (window._context.isDirty) {
        var msg = window._resourcesManager.getGlobalResource("RES_WEBFORM_DirtyMessage");
        e.returnValue = msg;     // Gecko, Trident, Chrome 34+
        return msg;
    }
};
interface Window { scope_ResultsForm: DigicircMatchmaking.Controllers.IResultsFormScope; }
namespace DigicircMatchmaking.Controllers {
const ResultsFormConditionalFormattings = [
        ];
const ResultsFormDataValidations = [
                                   ];
const ResultsFormCalculatedExpressions = [
        ];

// Datasets Column Info
export var CompanyDataSet_ColumnInfo = [new Joove.ColumnInfo("Name", "string", null, false),
                                        new Joove.ColumnInfo("Email", "string", null, false),
                                        new Joove.ColumnInfo("Address.Country.Name", "string", null, false)];
window["CompanyDataSet_ColumnInfo"] = CompanyDataSet_ColumnInfo;

// Datasets Projection Shemas
window["Table_ProjectionScema"] = {};


export interface IResultsFormScope extends Joove.IWebPageScope {
model:
    DigicircMatchmaking.ViewModels.ResultsForm.ResultsFormViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,

    },
    _masterValidations?: any;
}

class ResultsFormController {
private timeoutDelayLockButtonClicked:
    any;


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_ResultsForm.model = window.scope_ResultsForm.dehydrate();
        //window.scope_EmptyMasterPage.model = window.scope_ResultsForm.model;
        DigicircMatchmaking.ViewModels.ResultsForm.ResultsFormViewModel._lightCast(window.scope_ResultsForm.model);
        // Joove.Common.applyScope(window.scope_ResultsForm);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: IResultsFormScope, $timeout: ng.ITimeoutService) {
        window.scope_ResultsForm = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DigicircMatchmaking.ViewModels.ResultsForm.ResultsFormViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_EmptyMasterPage._validations;
        $scope._validations = { summary:
                                [],
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: IResultsFormScope, $timeout: ng.ITimeoutService) {


        $scope.model = DigicircMatchmaking.ViewModels.ResultsForm.ResultsFormViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_EmptyMasterPage.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);




        //}

        $.connection['eventsHub'].on('__connectedEvent', () => { });

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start().then(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {
        };
// Controller actions
        $scope.actions = {
Index:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("ResultsForm", "Index", "GET", [], {}, _modalOptions);
            },
        };
// Events
        $scope.eventCallbacks = {
ButtonClicked:
            (e, DataItem, _parents) => {


                if (e != null) {
                    e.stopPropagation();
                    e.preventDefault();
                }

                if (this.timeoutDelayLockButtonClicked != null) {
                    $timeout.cancel(this.timeoutDelayLockButtonClicked);
                }

                this.timeoutDelayLockButtonClicked = $timeout(() => {
                    Joove.Common.setControlKeyPressed(e, 0); Joove.Common.setLastClickedElement(e);

                    Joove.Validation.Manager.validateFormAndExecute(() => {
                        //This is true only when the menu item is clicked programmatically
                        //and occurs when the middle mouse button click is triggered
                        var openInNewWindow = $(e.target).data("openInNewWindow") === true || undefined;
                        if (openInNewWindow) $(e.target).data("openInNewWindow", undefined); Joove.Core.executeRedirectControllerAction("ActorForm", "Edit", "GET", [Joove.Common.nullSafe<any>(() => _parents[0].Id, 0)], null, null, openInNewWindow);
}, { groups: [], withDataValidationsCheck: false, withRequiredFieldsCheck: false });
                }, 0);
            },

        };

// Rules
        window._ruleEngine.addDataValidations(ResultsFormDataValidations);
        window._ruleEngine.addConditionalFormattings(ResultsFormConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(ResultsFormCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_EmptyMasterPage as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("ResultsFormController", ["$scope", "$timeout", ResultsFormController] as Array<string>);
}
