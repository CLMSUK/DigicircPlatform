// Copyright (c) CLMS UK. All rights reserved.
// Licensed under the Custom License. See LICENSE file in the project root for full license information.
// This was autogenerated by zAppDev.
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
$(document).keyup((e) => {
    if (e.which === 13 && Joove.Common.eventPreventsDefaultFormAction(e as JQueryEventObject) === false && !$(e.target).parent().hasClass("search-element")) {
        window._popUpManager.previouslyFocusedElement = document.activeElement;
        $(":focus").blur();
        Joove.Core.getScope().eventCallbacks.btnChangePasswordClicked(e);
    }
});
interface Window { scope_ForgotUsername: DigicircMatchmaking.Controllers.IForgotUsernameScope; }
namespace DigicircMatchmaking.Controllers {
const ForgotUsernameConditionalFormattings = [
        ];
const ForgotUsernameDataValidations = [
new Joove.JbRule({
name: "EmailIsEmptyValidation",
type: Joove.RuleTypes.DataValidation,
evaluatedAtServer: false,
isDataSetRule: false,
group: "",



condition: async (_parents) => {
        return Joove.Common.nullSafe<any>(() => CLMS.Framework.String.IsNullOrEmpty(Joove.Common.nullSafe(function () {
            return Joove.Common.nullSafe<any>(() => window["scope_ForgotUsername"].model.txtEmail, "").trim();
        }, "")), false);


    },
expression: async function(_parents) {
        return Joove.Common.nullSafe<any>(() => window._resourcesManager.getLocalResource("RES_DATAVALIDATION_MESSAGE_EmailIsEmpty", false), "");
    },
dataValidationMessageType:  Joove.DataValidationMessageType.ERROR,
evaluationTimes: [Joove.EvaluationTimes.OnSubmit],
originalName: 'EmailIsEmpty',
fromMasterPage: false
}),
                                      ];
const ForgotUsernameCalculatedExpressions = [
        ];

// Datasets Column Info

// Datasets Projection Shemas


export interface IForgotUsernameScope extends Joove.IWebPageScope {
model:
    DigicircMatchmaking.ViewModels.ForgotUsername.ForgotUsernameViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,
EmailIsEmpty: Joove.Validation.BindingEntry,txtUsername: Joove.Validation.BindingEntry,
    },
    _masterValidations?: any;
}

class ForgotUsernameController {
private timeoutDelayLockButton1Clicked:
    any;
private timeoutDelayLockbtnChangePasswordClicked:
    any;


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_ForgotUsername.model = window.scope_ForgotUsername.dehydrate();
        //window.scope_MasterPageSignIn.model = window.scope_ForgotUsername.model;
        DigicircMatchmaking.ViewModels.ForgotUsername.ForgotUsernameViewModel._lightCast(window.scope_ForgotUsername.model);
        // Joove.Common.applyScope(window.scope_ForgotUsername);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: IForgotUsernameScope, $timeout: ng.ITimeoutService) {
        window.scope_ForgotUsername = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DigicircMatchmaking.ViewModels.ForgotUsername.ForgotUsernameViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_MasterPageSignIn._validations;
        $scope._validations = { summary:
                                [],
                                EmailIsEmpty:
                                { rule: ForgotUsernameDataValidations[0], group: ForgotUsernameDataValidations[0].group },
                                txtUsername:
                                { requiredControlId: 'txtUsername', group: null },
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: IForgotUsernameScope, $timeout: ng.ITimeoutService) {


        $scope.model = DigicircMatchmaking.ViewModels.ForgotUsername.ForgotUsernameViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_MasterPageSignIn.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);




        //}

        $.connection['eventsHub'].on('__connectedEvent', () => { });

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start().then(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {
        };
// Controller actions
        $scope.actions = {
Render:
            async (fromMatching, _cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("ForgotUsername", "Render", "GET", [fromMatching], {}, _modalOptions);
            },
ResetPasswordRequest:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();
                await window._ruleEngine.update(Joove.EvaluationTimes.OnSubmit);

                window._context.isDirty = false;
                Joove.Core.executeControllerActionNew({
verb: 'POST', controller: 'ForgotUsername', action: 'ResetPasswordRequest',
queryData: [], postData: {'model': $scope.model},
cb: _cb, modalOptions: _modalOptions, event: _e
                });
            },
        };
// Events
        $scope.eventCallbacks = {
Button1Clicked:
            (e, DataItem, _parents) => {


                if (e != null) {
                    e.stopPropagation();
                    e.preventDefault();
                }

                if (this.timeoutDelayLockButton1Clicked != null) {
                    $timeout.cancel(this.timeoutDelayLockButton1Clicked);
                }

                this.timeoutDelayLockButton1Clicked = $timeout(() => {
                    Joove.Common.setControlKeyPressed(e, 0); Joove.Common.setLastClickedElement(e);

                    Joove.Validation.Manager.validateFormAndExecute(() => {
                        //This is true only when the menu item is clicked programmatically
                        //and occurs when the middle mouse button click is triggered
                        var openInNewWindow = $(e.target).data("openInNewWindow") === true || undefined;
                        if (openInNewWindow) $(e.target).data("openInNewWindow", undefined); Joove.Core.executeRedirectControllerAction("SignInPage", "Load", "GET", [Joove.Common.nullSafe<any>(() => window["scope_ForgotUsername"].model.FromMatching, false)], null, null, openInNewWindow);
}, { groups: [], withDataValidationsCheck: false, withRequiredFieldsCheck: false });
                }, 0);
            },

btnChangePasswordClicked:
            (e, DataItem, _parents) => {


                if (e != null) {
                    e.stopPropagation();
                    e.preventDefault();
                }

                if (this.timeoutDelayLockbtnChangePasswordClicked != null) {
                    $timeout.cancel(this.timeoutDelayLockbtnChangePasswordClicked);
                }

                this.timeoutDelayLockbtnChangePasswordClicked = $timeout(() => {
                    Joove.Common.setControlKeyPressed(e, 0); Joove.Common.setLastClickedElement(e);

                    Joove.Validation.Manager.validateFormAndExecute(() => {
                        $scope.actions.ResetPasswordRequest(null, null, e);
}, { groups: [Joove.Validation.Constants.ALL_GROUPS], withDataValidationsCheck: true, withRequiredFieldsCheck: true });
                }, 0);
            },

        };

// Rules
        window._ruleEngine.addDataValidations(ForgotUsernameDataValidations);
        window._ruleEngine.addConditionalFormattings(ForgotUsernameConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(ForgotUsernameCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_MasterPageSignIn as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("ForgotUsernameController", ["$scope", "$timeout", ForgotUsernameController] as Array<string>);
}
